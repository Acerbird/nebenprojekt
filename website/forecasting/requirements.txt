# Abhängigkeiten der Forecast-Modelle. Diese laufen offline und getrennt von der
# Website — deren Laufzeit bleibt bei FastAPI, Uvicorn und Jinja2.
#
# Die Versionen sind auf den Stand des Forschungsprojekts festgelegt, aus dem
# die Modelle stammen. Neuere Generationen (etwa pandas 3) ändern das Verhalten
# an Stellen, auf die sich die Modelle verlassen — damit Ergebnisse hier
# dieselben sind wie dort, bleibt es bei diesen Versionen.
#
# Eigene virtuelle Umgebung anlegen (Python 3.9, wie im Forschungsprojekt):
#   /usr/bin/python3 -m venv forecasting/.venv
#   forecasting/.venv/bin/pip install -r forecasting/requirements.txt
pandas==2.0.0
numpy==1.24.2
scipy==1.13.1
statsmodels==0.14.6
scikit-learn>=1.3,<1.7
pytz
python-dateutil
